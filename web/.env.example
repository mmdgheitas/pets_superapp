# Public base URL of the NestJS API (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3000

# When true, the storefront uses in-memory mock data and never calls the API.
# Default for local demos is true (see .env.local). Set false to hit a real backend.
NEXT_PUBLIC_USE_MOCK=true
